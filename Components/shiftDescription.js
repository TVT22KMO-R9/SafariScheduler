import React from 'react';
import { View, Text } from 'react-native';

const ShiftDescription = ({ text }) => {
  return (
    <View>
      <Text>{text}</Text>
    </View>
  );
};

export default ShiftDescription;